% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ source
\name{eu_joint}
\alias{eu_joint}
\title{Posterior predictive expected utility under the joint distribution}
\usage{
eu_joint(samples, M, util, J)
}
\arguments{
\item{samples}{Posterior sample matrix from \code{\link{sample_posterior}}.}
\item{M}{Number of predictive outcome draws per posterior sample.}
\item{util}{Utility function returning \eqn{U(x_T, x_E)} on the bounded scale, typically built by \code{\link{make_utility}}.}
\item{J}{Integer, number of dose levels.}
}
\value{
Numeric vector of length \eqn{J} giving estimated expected
utility at each dose.
}
\description{
Monte Carlo estimate of \eqn{\widehat U_n(d_j) =
E[U(g_T(Y_T), g_E(Y_E)) \mid d_j, \mathcal D_n]} at each dose
\eqn{d_j}, computed under the joint Gaussian working model. For each
posterior draw \eqn{\theta^{(s)}}, \code{M} predictive outcome pairs
are drawn from \eqn{\mathcal N_2(\mu^{(s)}(d_j), \Sigma_j^{(s)})}
with the within-dose correlation \eqn{\rho^{(s)}} applied; the
utility is averaged over the \eqn{SM} draws. This is the joint
posterior predictive criterion of the manuscript.
}
\seealso{
\code{\link{eu_marginal}}, \code{\link{eu_plugin}}.
}
