% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ source
\name{run_replicates}
\alias{run_replicates}
\title{Run many replicates of a single (scenario, design) cell}
\usage{
run_replicates(scen, design, n_reps = 400L, seed_base = 0L, parallel = FALSE, n_cores = 2L, ...)
}
\arguments{
\item{scen}{Scenario list.}
\item{design}{One of \code{"joint"}, \code{"marginal"}, \code{"plugin"}, \code{"boin12"}.}
\item{n_reps}{Number of replicates.}
\item{seed_base}{Integer seed offset; replicate \eqn{i} is seeded as \code{seed_base + i}.}
\item{parallel}{Logical; if \code{TRUE} and the \pkg{parallel} package is available, use \code{\link[parallel]{mclapply}}. Not used on Windows.}
\item{n_cores}{Number of worker processes when \code{parallel} is \code{TRUE}.}
\item{...}{Additional arguments passed to the underlying simulator.}
}
\value{
A list of length \code{n_reps}, each element the return
value of \code{\link{simulate_trial}} or
\code{\link{boin12_trial}}.
}
\description{
Runs \code{n_reps} replicates of either \code{\link{simulate_trial}}
(for the continuous designs \code{"joint"}, \code{"marginal"},
\code{"plugin"}) or \code{\link{boin12_trial}} (for
\code{design = "boin12"}) under a single scenario. Replicates are
seeded reproducibly from \code{seed_base} so that paired
within-replicate comparisons across designs are possible.
}
\examples{
\dontrun{
scen <- ppod_scenarios$S4b
cell <- run_replicates(scen, design = "joint", n_reps = 20)
length(cell)
}
}
